#include "requests.h"

respone Requests::get(string url)
{
    
}
respone Requests::post(string url,map<string, string> data) 
{

}

int main ()
{
    string url = "http://www.baidu.com";
    respone res = requests.get(url);
}
